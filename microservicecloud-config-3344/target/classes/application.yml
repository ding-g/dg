server:
  port: 3344
spring:
  application:
      name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ding-g/dg/tree/master/microservicecloud-config
